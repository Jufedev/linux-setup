*.log
*.swp
*.swo
*~
.DS_Store

# Artefactos locales de tooling (gentle-ai / skills) — no van al repo
.atl/
.claude/

# Llaves SSH — nunca subir credenciales al repo
*.pem
id_*
*_ed25519
*_rsa

# Fuentes descargadas sueltas en la raíz — se instalan vía --fonts, no van al repo
/*.ttf
/*.otf
